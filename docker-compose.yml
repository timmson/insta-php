version: '2'
services:
   instagram-api:
     build:
       context: .
     image: instagram-api
     container_name: php-instagram-server
     restart: always

networks:
 default:
   external:
     name: mynet